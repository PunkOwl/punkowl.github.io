import React, { useState } from 'react';
import Typist from 'react-typist';

export default function Header() {

    let i = 0;
    const textList = [
        'Animate this text',
        'Animate this textdsa'
    ]

    return(
        <div className="header-space">
            
        </div>
    );

}